/// Enums for different app states and values
library;

enum ProductType { physical, digital }

enum OrderStatus { pending, processing, shipped, delivered, cancelled }

enum PaymentMethod { creditCard, paypal, cashOnDelivery }

enum ThemeModeType { light, dark, liquidGlass }
